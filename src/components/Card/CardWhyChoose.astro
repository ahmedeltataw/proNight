---
import { Image } from "astro:assets";

interface Props {
    src:string | any;
    title:string;
  aos?:{
        effect:string |any,
        time:string |any,
        delay:string |any,
    }
}
const { src ,title,aos=false } = Astro.props;
---
<div class="whyChooseCard p-14 round-5 text-center" {...aos && { 'data-aos': aos.effect, 'data-aos-duration': aos.time ,'data-aos-delay': aos.delay }}>
    <div class="ImgContainer p-9 round-5 mb-8">
        <Image src={src} alt={title} class="img-contain w-20 h-20 mx-auto" quality={70} />
    </div>
    <h5 class="fs-24 fw-700 mb-8">{title}</h5>
    <p class="fs-16 o-80">
        عملية اختبار الأعمال الإلكترونية المكتملة بالكامل مع خدمة عملاء مبنية على البحث الكامل. محتوى عالمي شامل وذو جودة.
    </p>
</div>